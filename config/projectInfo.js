const project = {
    /* Copy the URL's ID of your Figma file */
    fileID: "<FILE_ID>",
    /* Create a Figma Project Token (Menu => Account Settings => Personal Access Tokens) then insert here */
	projectToken : "<PROJECT_TOKEN>"
}

module.exports = project;